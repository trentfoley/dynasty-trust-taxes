---
phase: 03-correct-issues-found-during-external-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/2025.json
  - fill_1041.py
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03

must_haves:
  truths:
    - "Form 8960 Line 19a uses gross income ($105,140.67), not taxable income ($105,040.67)"
    - "NIIT amount equals $3,400.65 (not $3,396.85)"
    - "Schedule B Line 6 is filled with -$104,236.73 (negative net capital gain)"
    - "Schedule B DNI (Line 7) equals $903.94"
    - "Line 14 (attorney/accountant fees) reads from config/2025.json and wires into taxable income"
    - "Zero deduction case produces no change to Schedule G or NIIT beyond the FIX-01 correction"
  artifacts:
    - path: "config/2025.json"
      provides: "deductions.attorney_accountant_fees key (value 0.00)"
      contains: "deductions"
    - path: "fill_1041.py"
      provides: "All three bug fixes applied"
      exports: ["compute_form_1041_page1", "compute_form_8960", "compute_schedule_b", "build_field_maps"]
  key_links:
    - from: "config/2025.json"
      to: "compute_form_1041_page1()"
      via: "cfg.get('deductions', {}).get('attorney_accountant_fees', 0.0)"
      pattern: "attorney_accountant_fees"
    - from: "compute_form_1041_page1()"
      to: "taxable_income"
      via: "total_income - total_deductions_line16 - exemption"
      pattern: "taxable_income"
    - from: "compute_form_8960()"
      to: "page1['total_income']"
      via: "agi_undistributed_nii = page1['total_income']"
      pattern: "total_income"
    - from: "compute_schedule_b()"
      to: "capital_gains_subtraction"
      via: "-page1['capital_gain_loss']"
      pattern: "capital_gains_subtraction"
---

<objective>
Apply three precision bug fixes to fill_1041.py and config/2025.json identified during external review of the v1.0 output forms.

Purpose: Correct two computational errors (Form 8960 used taxable income instead of gross income; Schedule B Line 6 was blank) and add config-driven deduction support (Line 14) so the return is audit-ready and computationally correct.

Output: Updated fill_1041.py and config/2025.json. Dry-run output shows NIIT = $3,400.65 and Schedule B DNI = $903.94.
</objective>

<execution_context>
@C:/Users/TrentFoley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-correct-issues-found-during-external-review/03-CONTEXT.md
@.planning/phases/03-correct-issues-found-during-external-review/03-RESEARCH.md

<interfaces>
<!-- Key code contracts extracted from fill_1041.py and config files. -->
<!-- Executor uses these directly — no exploration needed. -->

Computation chain (sequential):
  parse_csv → compute_schedule_d → compute_form_1041_page1
    → compute_schedule_b → compute_schedule_g → compute_schedule_d_part5
    → compute_form_8960 → build_field_maps → fill PDFs

compute_form_1041_page1(cfg, csv_data) returns dict including:
  - total_income       # gross income (Form 1041 Line 17) = dividends + interest + capital_gain_loss
  - taxable_income     # total_income - deductions - exemption (currently: total_income - $100)
  - capital_gain_loss  # = sched_d["net_combined"] (positive = gain)
  - exemption          # $100 complex trust exemption from config

compute_schedule_b(page1) — current signature, takes page1 dict
  - currently returns: adjusted_total_income, accounting_income, distributable_net_income,
    income_required_to_be_distributed, other_amounts_distributed, total_distributions,
    income_distribution_deduction
  - MISSING: capital_gains_subtraction, and distributable_net_income is currently wrong

compute_form_8960(cfg, page1) — Line 445:
  - CURRENT (WRONG): agi_undistributed_nii = page1["taxable_income"]
  - CORRECT:         agi_undistributed_nii = page1["total_income"]

build_field_maps() — assembles p1_mappings, sched_b_mappings, etc.
  - p1_mappings already handles income lines and exemption
  - sched_b_mappings currently has: adjusted_total_income, accounting_income,
    income_required_to_be_distributed, other_amounts_distributed, total_distributions,
    income_distribution_deduction, distributable_net_income
  - MISSING from sched_b_mappings: "gain_from_page1_line4"

Field IDs confirmed from config/2025_fields.json:
  - attorney_accountant_fees  → topmostSubform[0].Page1[0].f1_32[0]
  - total_deductions           → topmostSubform[0].Page1[0].f1_35[0]
  - gain_from_page1_line4      → f2_13[0]  (Schedule B Line 6)
  - distributable_net_income   → f2_14[0]  (Schedule B Line 7)

Python path: /c/ProgramData/miniconda3/python (NOT python3 — Windows Store stub)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add deductions support (FIX-03)</name>
  <files>config/2025.json, fill_1041.py</files>
  <action>
In config/2025.json, add a "deductions" key after the "trust_exemption" entry:
```json
"deductions": {
    "attorney_accountant_fees": 0.00
}
```

In fill_1041.py, update compute_form_1041_page1():
- Read deductions from config: `deductions = cfg.get("deductions", {})`
- Extract attorney fees: `attorney_fees = round(float(deductions.get("attorney_accountant_fees", 0.0)), 2)`
- Compute Line 16 subtotal: `total_deductions_line16 = attorney_fees` (only Line 14 active for 2025)
- Update taxable_income computation to subtract deductions: `taxable_income = round(total_income - total_deductions_line16 - exemption, 2)`
- Add to the return dict: `"attorney_accountant_fees": attorney_fees` and `"total_deductions": total_deductions_line16`

In fill_1041.py, update build_field_maps() in the p1_mappings dict — add:
```python
"attorney_accountant_fees": computed.get("attorney_accountant_fees"),
"total_deductions":         computed.get("total_deductions"),
```

Per the research conservative approach (Claude's Discretion): do NOT reduce Form 8960 NII by attorney fees — Lines 9a-9c on Form 8960 are deferred to a future phase. This slightly overstates NIIT but is audit-safe.

Zero deduction handling: write $0.00 for zero values (consistent with how the $100 exemption is always written — do not skip zero-valued deduction fields).

Verify config/2025.json is valid JSON after edit.
  </action>
  <verify>
    <automated>/c/ProgramData/miniconda3/python fill_1041.py --dry-run 2>&1 | grep -i "attorney\|deductions\|taxable"</automated>
  </verify>
  <done>Dry-run output includes attorney_accountant_fees and total_deductions fields. With 0.00 fees, taxable_income remains $105,040.67 (unchanged from pre-fix baseline).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix Form 8960 Line 19a to use gross income (FIX-01)</name>
  <files>fill_1041.py</files>
  <action>
In fill_1041.py, locate compute_form_8960() — find the line at approximately line 445:
```python
# CURRENT (WRONG):
agi_undistributed_nii = page1["taxable_income"]
```

Change it to:
```python
# CORRECTED: Form 8960 Part III Line 19a for trusts = Form 1041 Line 17 (adjusted total income / gross income)
# IRS instructions explicitly say Line 19a = Line 17, NOT Line 23 (taxable income).
# The $100 complex trust exemption must NOT be subtracted before feeding Line 8960 Line 19a.
agi_undistributed_nii = page1["total_income"]
```

No other changes to compute_form_8960(). The cascade from total_income → agi_minus_threshold → lesser_of_nii_or_agi_excess → niit_amount all flow automatically.

After the fix with zero deductions:
- Line 19a = $105,140.67
- Line 19c = $105,140.67 - $15,650 = $89,490.67
- Line 20 = min($105,140.67, $89,490.67) = $89,490.67
- Line 21 NIIT = $89,490.67 × 0.038 = $3,400.65
  </action>
  <verify>
    <automated>/c/ProgramData/miniconda3/python fill_1041.py --dry-run 2>&1 | grep -i "niit\|8960\|3400\|3,400"</automated>
  </verify>
  <done>Dry-run output shows NIIT = $3,400.65 (not $3,396.85). The $3.80 increase confirms the fix is applied correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix Schedule B Line 6 capital gains subtraction (FIX-02)</name>
  <files>fill_1041.py</files>
  <action>
In fill_1041.py, update compute_schedule_b(page1):

Add computation of capital_gains_subtraction using page1["capital_gain_loss"] (which is already $104,236.73, the positive net gain from Schedule D):
```python
capital_gain = page1["capital_gain_loss"]           # positive gain from Sched D line 19
capital_gains_subtraction = -round(capital_gain, 2) # Line 6: negative per IRS instructions
                                                     # (Schedule B subtracts capital gains retained
                                                     #  in corpus to arrive at DNI)
```

Update distributable_net_income (Line 7) to subtract capital gains:
```python
# CURRENT (WRONG): distributable_net_income = page1["total_income"]  (or similar)
# CORRECTED:
dni = round(page1["total_income"] + capital_gains_subtraction, 2)  # Line 7
```

Add "capital_gains_subtraction" to the return dict. Update "distributable_net_income" in the return dict to use `dni`.

If capital_gain_loss is ever negative (loss year), capital_gains_subtraction becomes positive — which is correct IRS treatment (losses increase DNI). The formula handles both cases correctly.

In fill_1041.py, update build_field_maps() in the sched_b_mappings dict — add:
```python
"gain_from_page1_line4": computed.get("sched_b_capital_gains_subtraction"),
```

In the computed dict merge block (where sched_b keys are merged into computed), add:
```python
"sched_b_capital_gains_subtraction": sched_b["capital_gains_subtraction"],
```

The semantic key "gain_from_page1_line4" maps to AcroForm field f2_13[0] (Schedule B Line 6). The "distributable_net_income" key already exists in sched_b_mappings and maps to f2_14[0] — it will automatically pick up the corrected DNI value from the updated return dict.
  </action>
  <verify>
    <automated>/c/ProgramData/miniconda3/python fill_1041.py --dry-run 2>&1 | grep -i "distributable\|capital_gains_subtraction\|sched_b\|903\|104,236\|-104"</automated>
  </verify>
  <done>Dry-run output shows distributable_net_income = $903.94 and capital_gains_subtraction = -$104,236.73. Both values confirm Schedule B Line 6 and Line 7 are now correct.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run full dry-run and confirm all corrected values appear together:

```
/c/ProgramData/miniconda3/python fill_1041.py --dry-run
```

Expected values (with attorney_accountant_fees = 0.00):
- Form 8960 Line 19a: $105,140.67 (was $105,040.67)
- NIIT (Line 21): $3,400.65 (was $3,396.85)
- Total tax: ~$23,129.34 (was $23,125.19) — +$3.80 net increase
- Schedule B capital_gains_subtraction: -$104,236.73 (was blank/missing)
- Schedule B distributable_net_income: $903.94 (was $105,140.67)
- taxable_income: $105,040.67 (unchanged — zero deductions)
- attorney_accountant_fees: $0.00 (new field, zero)
- total_deductions: $0.00 (new field, zero)
</verification>

<success_criteria>
- [ ] config/2025.json contains "deductions": {"attorney_accountant_fees": 0.00}
- [ ] fill_1041.py compute_form_1041_page1() reads attorney_accountant_fees from config and subtracts from taxable_income
- [ ] fill_1041.py compute_form_8960() uses page1["total_income"] (not taxable_income) for Line 19a
- [ ] fill_1041.py compute_schedule_b() populates capital_gains_subtraction = -page1["capital_gain_loss"]
- [ ] fill_1041.py compute_schedule_b() computes dni = total_income + capital_gains_subtraction
- [ ] build_field_maps() includes attorney_accountant_fees, total_deductions in p1_mappings
- [ ] build_field_maps() includes gain_from_page1_line4 in sched_b_mappings (via sched_b_capital_gains_subtraction)
- [ ] Dry-run shows NIIT = $3,400.65
- [ ] Dry-run shows distributable_net_income = $903.94
</success_criteria>

<output>
After completion, create `.planning/phases/03-correct-issues-found-during-external-review/03-01-SUMMARY.md`
</output>
