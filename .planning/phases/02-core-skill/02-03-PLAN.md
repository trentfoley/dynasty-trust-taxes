---
phase: 02-core-skill
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - fill_1041.py
  - .claude/commands/fill-1041.md
autonomous: false
requirements:
  - PDF-02
  - PDF-03
  - PDF-04
  - PDF-05
  - PDF-06
  - PDF-07
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04

must_haves:
  truths:
    - "Running `python fill_1041.py` (live mode) writes separate filled PDFs to output/ for each form"
    - "output/2025_1041_filled.pdf exists after live run and all fields are visibly filled in browser PDF viewer"
    - "output/2025_sched_d_filled.pdf exists after live run (or warning printed if form is XFA)"
    - "output/2025_8960_filled.pdf exists after live run (or warning printed if form is XFA)"
    - "output/2025_8949_filled.pdf exists after live run (or warning printed if form is XFA)"
    - "Running `/fill-1041 --dry-run` from Claude chat invokes fill_1041.py with --dry-run flag"
    - "Running `python fill_1041.py --dry-run` writes zero files to output/"
  artifacts:
    - path: "fill_1041.py"
      provides: "Complete skill: CSV parse, compute, validate, fill PDFs, print summary"
    - path: ".claude/commands/fill-1041.md"
      provides: "Claude slash command definition for /fill-1041"
    - path: "output/2025_1041_filled.pdf"
      provides: "Filled Form 1041 (all 3 pages: Page 1, Schedule B, Schedule G)"
  key_links:
    - from: "fill_1041.py main()"
      to: "fill_pdf.py"
      via: "subprocess.run([python_exe, 'fill_pdf.py', '--fields', tmp, '--form', form, '--output', out])"
      pattern: "subprocess\\.run.*fill_pdf\\.py"
    - from: ".claude/commands/fill-1041.md"
      to: "fill_1041.py"
      via: "/c/ProgramData/miniconda3/python fill_1041.py $ARGUMENTS"
      pattern: "fill_1041\\.py"
---

<objective>
Wire the live PDF output path in fill_1041.py by implementing fill_form() and updating main() to call fill_pdf.py for each form. Create the .claude/commands/fill-1041.md slash command. Finish with a human verification checkpoint to visually confirm all filled PDFs render correctly in a browser PDF viewer.

Purpose: Completes the skill end-to-end. After this plan, a single `/fill-1041` invocation from Claude chat produces print-ready filled PDFs for the entire Form 1041 filing package.

Output: fill_1041.py with live PDF writing, .claude/commands/fill-1041.md slash command, and 4 filled PDFs in output/.
</objective>

<execution_context>
@C:/Users/TrentFoley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-skill/02-CONTEXT.md
@.planning/phases/02-core-skill/02-RESEARCH.md
@.planning/phases/02-core-skill/02-01-SUMMARY.md
@.planning/phases/02-core-skill/02-02-SUMMARY.md

<interfaces>
<!-- Key contracts from Phase 1 and prior plans. -->

Python executable: /c/ProgramData/miniconda3/python (NOT python3)

fill_pdf.py CLI (Phase 1 artifact -- do NOT modify):
  /c/ProgramData/miniconda3/python fill_pdf.py --fields json_file --form blank_pdf --output output_pdf
  Exits 0 on success, non-zero on unknown field names or write errors.

From fill_1041.py (Plans 01+02 -- functions to USE, not rewrite):
  parse_args() -> Namespace with --dry-run, --year, --csv, --output-dir
  load_config(year) -> dict
  load_fields(year) -> dict
  parse_csv(csv_path) -> { div_1a, div_1b, int_box1, transactions }
  compute_schedule_d(transactions) -> { st_net, lt_net, net_combined, rows, ... }
  compute_form_1041_page1(csv_data, sched_d, cfg) -> { taxable_income, total_income, ... }
  compute_schedule_b(page1) -> { adjusted_total_income, distributions, ... }
  compute_schedule_g(taxable_income, qual_div, lt_net, cfg) -> { tax_on_taxable_income, total_tax, ... }
  compute_form_8960(csv_data, page1, sched_d, cfg) -> { niit_amount, ... }
  validate(csv_data, computed, tolerance=1.00) -> list of mismatch strings, or empty list
  build_field_maps(computed, fields) -> { 'form_1041': {...}, 'schedule_d': {...}, 'form_8960': {...}, 'form_8949': {...} }
  print_header(args, cfg)
  print_summary(page1, sched_g, form_8960, output_paths)

Output file naming convention (from CONTEXT.md locked decisions):
  output/2025_1041_filled.pdf     <- f1041.pdf (all 3 pages: Page 1, Schedule B, Schedule G)
  output/2025_sched_d_filled.pdf  <- f1041sd.pdf
  output/2025_8960_filled.pdf     <- f8960.pdf
  output/2025_8949_filled.pdf     <- f8949.pdf
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement fill_form() and live PDF writing in main()</name>
  <files>fill_1041.py</files>
  <action>
Add fill_form() and complete the live mode branch in main(). Do NOT modify any existing function except main().

Implement fill_form(field_values, form_path, output_path): Write field_values to a temporary JSON file using tempfile.NamedTemporaryFile, call fill_pdf.py via subprocess.run with args ['/c/ProgramData/miniconda3/python', 'fill_pdf.py', '--fields', tmp_path, '--form', form_path, '--output', output_path], delete the temp file in a finally block. Raise RuntimeError if returncode != 0, including the stderr text. On success print "PDF written: {output_path}".

Update main() to:
1. After computing field_maps (already done in Plan 02), build output_paths dict: form_1041 -> output/{year}_1041_filled.pdf, schedule_d -> output/{year}_sched_d_filled.pdf, form_8960 -> output/{year}_8960_filled.pdf, form_8949 -> output/{year}_8949_filled.pdf.
2. Build form_pdfs dict: form_1041 -> cfg['paths']['blank_form'] (forms/f1041.pdf), schedule_d -> forms/f1041sd.pdf, form_8960 -> forms/f8960.pdf, form_8949 -> forms/f8949.pdf.
3. In the live mode branch, loop over ['form_1041', 'schedule_d', 'form_8960', 'form_8949']: get fmap from field_maps; if empty, print "WARNING: No AcroForm fields for {form_key} -- skipping PDF output" and continue; else call fill_form(fmap, form_pdfs[form_key], output_paths[form_key]).
4. After the loop, call print_summary(page1, sched_g, form_8960, list(output_paths.values())).

Ensure field_maps is computed before the dry-run/live branch split in main():
  computed = {**csv_data, **page1, **sched_d, **sched_b, **sched_g, **form_8960}
  field_maps = build_field_maps(computed, fields)
  </action>
  <verify>
    <automated>cd /c/Users/TrentFoley/Source/dynasty-trust-taxes && /c/ProgramData/miniconda3/python fill_1041.py 2>&1 && ls output/2025_*_filled.pdf 2>&1</automated>
  </verify>
  <done>
`python fill_1041.py` (live mode) exits 0 and output/ contains at least output/2025_1041_filled.pdf. Other filled PDFs present unless their forms are XFA (in which case WARNING lines are printed for each skipped form).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .claude/commands/fill-1041.md slash command</name>
  <files>.claude/commands/fill-1041.md</files>
  <action>
Create the directory .claude/commands/ if it does not exist. Then create .claude/commands/fill-1041.md with the following content exactly (replace the bash fence markers appropriately):

Frontmatter: description = "Fill Form 1041 and all required attachments from brokerage CSV"

Body: A brief description followed by a bash code block containing:
  cd /c/Users/TrentFoley/Source/dynasty-trust-taxes && /c/ProgramData/miniconda3/python fill_1041.py $ARGUMENTS

Then a list of supported flags:
  --dry-run: print all computed values without writing any files
  --year YYYY: use config/YYYY.json (default: 2025)
  --csv PATH: override CSV input path
  --output-dir PATH: override output directory

Note in the file: The cd command is required because fill_1041.py uses relative paths (config/, forms/, output/) and must run from the project root.
  </action>
  <verify>
    <automated>cat /c/Users/TrentFoley/Source/dynasty-trust-taxes/.claude/commands/fill-1041.md 2>&1 && echo "FILE_EXISTS"</automated>
  </verify>
  <done>.claude/commands/fill-1041.md exists with description frontmatter and a bash block invoking fill_1041.py with $ARGUMENTS from the correct working directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual PDF Verification and Slash Command Test</name>
  <action>Human verification step -- no automated action. Claude has completed Tasks 1 and 2. User must visually inspect filled PDFs and test the slash command.</action>
  <verify>
    <automated>ls /c/Users/TrentFoley/Source/dynasty-trust-taxes/output/2025_1041_filled.pdf 2>&1 && echo "MAIN_PDF_EXISTS"</automated>
  </verify>
  <done>User has visually confirmed all filled PDFs render correctly in browser PDF viewer, slash command works from Claude chat. Phase 2 complete.</done>
  <what-built>
Complete fill_1041.py skill: CSV parsing, Schedule D, Schedule G (QD&CG worksheet), Schedule B, Form 8960 NIIT, validation, dry-run output, and live PDF writing for all 4 forms. .claude/commands/fill-1041.md slash command.
  </what-built>
  <how-to-verify>
Step 1 -- Dry-run pre-flight check:
Run: `python fill_1041.py --dry-run`
- Confirm header shows CSV path, blank form path, output dir, mode=DRY RUN
- Confirm all 4 progress lines: "Parsed CSV", "Computed Schedule D", "Computed Schedule G", "Computed Form 8960"
- Confirm "Validation passed"
- Confirm computation steps show: ordinary dividends=$903.82, interest=$0.12, ST net=$126.97, LT net=$104,109.76, taxable income=$105,040.67, Schedule G tax~=$19,722.93, NIIT~=$3,396.85
- Confirm field-value mapping section shows 4 form groups
- Confirm NO files written to output/ during dry-run

Step 2 -- Live run:
Run: `python fill_1041.py`
- Confirm progress lines for each step
- Confirm "Validation passed"
- Confirm "PDF written: output/2025_1041_filled.pdf" (and other forms if AcroForm)
- Confirm summary shows gross income, taxable income, Schedule G tax, NIIT, total tax, output paths

Step 3 -- Visual PDF inspection:
Open each filled PDF in a browser PDF viewer and confirm all fields are visibly filled:
- output/2025_1041_filled.pdf -- Page 1: income lines filled. Pages 2-3: Schedule B ($0 distribution) and Schedule G (tax lines) filled
- output/2025_sched_d_filled.pdf -- if it exists: ST and LT summary lines filled
- output/2025_8960_filled.pdf -- if it exists: NII lines and NIIT amount filled
- output/2025_8949_filled.pdf -- if it exists: Box A and Box D rows filled
For any form that printed WARNING, note it as XFA and accept as correct behavior.

Step 4 -- Slash command test:
From Claude chat, run: `/fill-1041 --dry-run`
- Confirm it invokes fill_1041.py and produces the same dry-run output as Step 1
  </how-to-verify>
  <resume-signal>Type "approved" if all visually inspected PDFs show filled fields, or describe any issues (wrong values, blank fields, missing forms)</resume-signal>
</task>

</tasks>

<verification>
Phase 2 complete when:
- `python fill_1041.py --dry-run` produces full pre-flight output with all correct values
- `python fill_1041.py` produces filled PDFs in output/
- output/2025_1041_filled.pdf opens in browser with all fields visibly filled
- `/fill-1041 --dry-run` from Claude chat works
- All 6 ROADMAP Phase 2 success criteria met:
  1. CSV parsed correctly (div=$903.82, int=$0.12, LT=$104,109.76, ST=$126.97)
  2. Schedule G computed via QD&CG worksheet
  3. NIIT = 3.8% of lesser of NII and (AGI minus $15,650)
  4. Field mapping covers all fields from 2024 reference return; fill_pdf.py called
  5. Dry-run prints all values without writing files
  6. Filled PDF opens with visible fields
</verification>

<success_criteria>
- Live run produces output/2025_1041_filled.pdf and any non-XFA attachment PDFs
- Filled PDFs visually verified in browser PDF viewer with all fields visible
- /fill-1041 slash command works from Claude chat
- --dry-run writes zero files
- All ROADMAP Phase 2 success criteria confirmed by human verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-skill/02-03-SUMMARY.md`
</output>
