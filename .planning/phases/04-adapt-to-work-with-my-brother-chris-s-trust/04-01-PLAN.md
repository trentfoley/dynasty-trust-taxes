---
phase: 04-adapt-to-work-with-my-brother-chris-s-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fill_1041.py
  - config/2025_trent.json
  - config/2025_chris.json
  - 2025_chris/dividends.csv
autonomous: true
requirements: [MULTI-01, MULTI-02, MULTI-03, MULTI-04]

must_haves:
  truths:
    - "fill_1041.py accepts --trust flag (required) to select which trust to process"
    - "config/2025_chris.json contains Chris's trust identity, EIN, address, and $500 attorney fees"
    - "config/2025_trent.json paths updated for renamed data directory (2025_trent/)"
    - "compute_schedule_g produces correct tax when qualified dividends exceed taxable income"
    - "Chris's manual CSV parses correctly through existing parse_csv state machine"
  artifacts:
    - path: "config/2025_chris.json"
      provides: "Chris trust config with identity, brackets, paths"
      contains: "87-6898455"
    - path: "config/2025_trent.json"
      provides: "Updated Trent config with corrected paths"
      contains: "2025_trent/"
    - path: "2025_chris/dividends.csv"
      provides: "Manual CSV with Chris's 1099-DIV data"
      contains: "9,045.68"
    - path: "fill_1041.py"
      provides: "Multi-trust CLI, refactored config loading, schedule_g fix"
      exports: ["load_config", "parse_args", "compute_schedule_g"]
  key_links:
    - from: "fill_1041.py parse_args"
      to: "load_config"
      via: "--trust flag parsed first, then config loaded as config/{year}_{trust}.json"
      pattern: "parse_known_args.*--trust"
    - from: "fill_1041.py compute_schedule_g"
      to: "taxable_income cap"
      via: "preferential_income capped at taxable_income before bucket distribution"
      pattern: "min.*preferential.*taxable"
---

<objective>
Set up multi-trust infrastructure: CLI flag, per-trust configs, Chris's data, and fix the schedule_g cap bug.

Purpose: Enable fill_1041.py to process either trust via `--trust trent` or `--trust chris`, with correct tax computation for dividends-only trusts where qualified dividends exceed taxable income.

Output: Working `--trust` flag, Chris's config + CSV, Trent's updated paths, fixed schedule_g.
</objective>

<execution_context>
@C:/Users/TrentFoley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adapt-to-work-with-my-brother-chris-s-trust/04-CONTEXT.md
@.planning/phases/04-adapt-to-work-with-my-brother-chris-s-trust/04-RESEARCH.md

@fill_1041.py
@config/2025_trent.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Chris config, Chris CSV, rename data dirs, update Trent config paths</name>
  <files>config/2025_chris.json, 2025_chris/dividends.csv, config/2025_trent.json</files>
  <action>
1. Create `config/2025_chris.json` with Chris's trust identity and full config. Copy structure exactly from `config/2025_trent.json` but replace trust-specific fields:
   - name: "Christopher Foley Childrens 2021 Super Dynasty Trust"
   - ein: "87-6898455"
   - fiduciary_name_title: "Christopher B. Foley, Trustee"
   - address_street: "23606 Powder Mill Dr"
   - address_city: "Tomball"
   - address_state: "TX"
   - address_zip: "77377"
   - paths.csv_input: "2025_chris/dividends.csv"
   - paths.output_dir: "output/chris"
   - deductions.attorney_accountant_fees: 500.00
   - All bracket/threshold values IDENTICAL to Trent (same tax year). Duplicate them; do NOT create a shared config layer (deferred idea).

2. Create `2025_chris/dividends.csv` — minimal CSV that the existing `parse_csv()` state machine can process:
   ```
   Form 1099DIV
   Box,Description,Amount,Total
   1a,Ordinary dividends,,"$9,045.68"
   1b,Qualified dividends,"$8,671.00",
   ```
   No 1099-INT or 1099-B sections needed — parser returns 0.0 defaults for missing sections.

3. Use `git mv 2025/ 2025_trent/` to rename Trent's data directory (preserves git history). Note: `2025_trent/` already exists with the files — if git mv fails because the directory was already renamed, skip this step.

4. Update `config/2025_trent.json`:
   - Change `paths.csv_input` from `"2025/XXXX-X123.CSV"` to `"2025_trent/XXXX-X123.CSV"`
   - Change `paths.output_dir` from `"output"` to `"output/trent"`

5. Delete `config/2025.json` if it still exists (it shows as deleted in git status already).
  </action>
  <verify>
    <automated>/c/ProgramData/miniconda3/python -c "import json; c=json.load(open('config/2025_chris.json')); assert c['trust']['ein']=='87-6898455'; assert c['paths']['csv_input']=='2025_chris/dividends.csv'; assert c['deductions']['attorney_accountant_fees']==500; t=json.load(open('config/2025_trent.json')); assert '2025_trent/' in t['paths']['csv_input']; assert t['paths']['output_dir']=='output/trent'; print('OK')"</automated>
  </verify>
  <done>Chris config exists with correct EIN/address/fees, Chris CSV exists with 1099-DIV data, Trent config paths updated to 2025_trent/ and output/trent</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor CLI for --trust flag and fix compute_schedule_g cap bug</name>
  <files>fill_1041.py</files>
  <behavior>
    - --trust flag is required; omitting it prints error and exits
    - --trust trent loads config/2025_trent.json
    - --trust chris loads config/2025_chris.json
    - compute_schedule_g with Chris's numbers (taxable=8445.68, qd=8671, lt=0) returns tax=779.35 (not 824.41)
    - compute_schedule_g with Trent's numbers still returns the same tax as before (regression)
    - Trent dry-run produces identical output to pre-refactor
  </behavior>
  <action>
1. **Fix compute_schedule_g cap bug** (line ~357 in fill_1041.py):
   Change the twenty_bucket calculation to cap preferential_income at taxable_income:
   ```python
   # Cap preferential income at taxable income (prevents overflow when QD > taxable)
   capped_preferential = min(preferential_income, taxable_income)
   twenty_bucket = max(0.0, capped_preferential - zero_bucket - fifteen_bucket)
   ```
   This is the fix identified in 04-RESEARCH.md Pitfall 1. Without it, Chris's tax is $824.41 instead of the correct $779.35.

2. **Refactor argument parsing to two-phase approach:**
   - Create `parse_trust_and_year()` function that uses `parse_known_args` to extract `--trust` (required, choices=["trent", "chris"]) and `--year` (default 2025) before loading config.
   - Update `parse_args(cfg)` to also accept and re-declare `--trust` for help text consistency.
   - In `main()`, call `parse_trust_and_year()` first, then `load_config(year, trust_name)`, then `parse_args(cfg)`.

3. **Refactor load_config:**
   Change signature from `load_config(year=2025)` to `load_config(year, trust_name)`.
   Path becomes `config/{year}_{trust_name}.json`.
   Remove the default year parameter — it comes from CLI parsing now.

4. **Update load_fields:**
   Keep `load_fields(year)` unchanged — fields catalog is shared across trusts (same IRS forms). The `year` parameter still comes from CLI.

5. **Update main() flow:**
   ```python
   def main():
       trust_name, year = parse_trust_and_year()
       cfg = load_config(year, trust_name)
       fields = load_fields(year)
       args = parse_args(cfg)
       # ... rest unchanged for now (conditional form gen is Plan 02)
   ```
   Remove the "reload if different year" block since year is parsed upfront.

6. **Update the module docstring** to reflect new usage:
   ```
   Usage:
       python fill_1041.py --trust trent [--year YYYY] [--csv PATH] [--output-dir PATH] [--dry-run]
       python fill_1041.py --trust chris [--year YYYY] [--dry-run]
   ```

7. **Verify Trent regression:** Run `--trust trent --dry-run` and confirm total tax is still $23,120.87.
8. **Verify Chris compute_schedule_g:** Run `--trust chris --dry-run` and confirm Schedule G tax is $779.35 (not $824.41).
  </action>
  <verify>
    <automated>/c/ProgramData/miniconda3/python fill_1041.py --trust trent --dry-run 2>&1 | grep -q "23,121" && /c/ProgramData/miniconda3/python fill_1041.py --trust chris --dry-run 2>&1 | grep -q "779" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>--trust flag works for both trusts, Trent total tax = $23,120.87 (unchanged), Chris Schedule G tax = $779.35 (correct, not $824.41), dry-run output succeeds for both trusts</done>
</task>

</tasks>

<verification>
1. `python fill_1041.py --trust trent --dry-run` succeeds with total tax $23,120.87
2. `python fill_1041.py --trust chris --dry-run` succeeds with total tax $779.35
3. `python fill_1041.py --dry-run` (no --trust) exits with error
4. Chris config has correct EIN, address, $500 fees
5. Trent config has updated paths (2025_trent/, output/trent)
</verification>

<success_criteria>
Both trusts process through dry-run with correct tax computation. Trent's numbers are unchanged (regression safe). Chris's Schedule G bug is fixed ($779.35 not $824.41). The --trust flag is required and routes to the correct config.
</success_criteria>

<output>
After completion, create `.planning/phases/04-adapt-to-work-with-my-brother-chris-s-trust/04-01-SUMMARY.md`
</output>
